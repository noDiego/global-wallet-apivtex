export enum TransactionStatus {
    APPROVED = 'APPROVED',
    REJECTED = 'REJECTED',
    INIT = 'INIT',
    CANCELED = 'CANCELED',
}

export enum VtexStatus {
    APPROVED = 'approved',
    DENIED = 'denied',
    UNDEFINED = 'undefined',
}
